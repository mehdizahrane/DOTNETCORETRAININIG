@model CoreTraining.Models.Category
@{
    ViewData["Title"] = "Edit category ";
}
<div class="pull-left">
    <a href="@Url.Action("Index","Category")")><i class="icon fa-arrow-left"></i> Back to categories list</a>
</div>
<br>
@if(ViewData["msg"] != null)
{
    <p class="alert alert-warning">
        @ViewData["msg"].ToString()
    </p>
}
<div class="container">
    <div class="row">
        <div class="col-md-12">
            <form asp-controller="Category" asp-action="Update" method="POST">
                <input type="hidden" asp-for="ID" value="@Model.ID">
                <div class="form-group">
                    <label asp-for="Name" class="col-md-2 control-label" ></label>
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger" ></span>
                </div>
                <div class="form-group">
                    <input type="submit" class="form-control btn btn-primary" value="Update" />
                </div>
            </form>
        </div>
    </div>
</div>